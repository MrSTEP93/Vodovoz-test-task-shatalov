<UserControl x:Class="Vodovoz.UI.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Vodovoz.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Список клиентов" FontSize="24" FontWeight="Bold" 
            Margin="0,0,0,15"/>
            <Button Content="Добавить" 
            Command="{Binding AddOrderCommand}" 
            Width="120" 
            Margin="20,0,0,0"
            Padding="8,4"/>
            <Button Content="Изменить" 
            Command="{Binding EditOrderCommand}" 
            Width="120" 
            Margin="20,0,0,0"
            Padding="8,4"/>
            <Button Content="Удалить" 
            Command="{Binding DeleteOrderCommand}" 
            Width="120" 
            Margin="20,0,0,0"
            Padding="8,4"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Orders}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single" Grid.Row="1"
                  SelectedItem="{Binding SelectedOrder}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="*"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Sum}" Width="*"/>
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding Employee.Surname}" Width="*"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Client.Name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
